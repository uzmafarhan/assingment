var userNames = ["admin", "ahmad", "ali", "hammad", "nasir"];
if (userNames.length === 0) {
    console.log("we need  to find some users");
}
else {
    userNames = [];
    console.log("all users has been removed", userNames.length);
}
